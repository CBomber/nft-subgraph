{
  "name": "cbomber-base-test",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ cbomber-base-test",
    "create-local": "graph create --node http://localhost:8020/ cbomber-base-test",
    "remove-local": "graph remove --node http://localhost:8020/ cbomber-base-test",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 cbomber-base-test",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.91.1",
    "@graphprotocol/graph-ts": "0.32.0"
  },
  "devDependencies": { "matchstick-as": "0.5.0" }
}
